using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace {0}
{
    [DisallowMultipleComponent]
    [AddComponentMenu("{2}/Dialog/{1}")]
	[ExecuteInEditMode]
	public class {1} : BaseDialog
	{
		protected override void Awake ()
        {
            base.Awake();
        }

        public override void Transmit(Hashtable param){
            base.Transmit(param);
        }

        public override void SetSize(Vector2 size)
        {
            base.SetSize(size);
        }

        protected override bool CheckReturnable(){
            return base.CheckReturnable();
        }

        protected override bool CheckBackable(){
            return base.CheckBackable();
        }
	}
}