using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.Common.APILogic
{
	public class {0} : ApiLogic
	{
		static List<string> parameterNames;
		
		public {0}  (object obj) : base (obj)
		{
			this.ApiPath = PathConfig.API_URL + APIConstant.PREFIX + APIConstant.{1};
			this.RequestMethod = GameEngine.API.ERequestMethod.{2};
		}

		public override void OnComplete (string data)
		{
			base.OnComplete (data);
		}

		protected override void InitParams (params object[] args)
		{
			if(parameterNames==null){
				parameterNames = new List<string>();
				{3}
			}
			if (args != null && args.Length > 0) {
				for(int i=0;i<args.Length;i++){
					this.Parameters.Add (parameterNames[i], args [i].ToString ());
				}
			}
		}
	}
}
